{
  "name": "@deltanear/proto",
  "version": "1.0.0",
  "description": "Shared types and schemas for DeltaNEAR",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:unit": "jest",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linting configured'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}